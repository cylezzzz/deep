<Window x:Class="DeepSeeArch.UI.MediaViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Media Viewer" Width="900" Height="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Media Viewer" FontSize="20" Margin="10"/>
        <Border Grid.Row="1" x:Name="ImageContainer" Visibility="Collapsed">
            <Image x:Name="ImageControl" Stretch="Uniform"/>
        </Border>
        <wv2:WebView2 Grid.Row="1" x:Name="VideoWebView" Visibility="Collapsed"/>
    </Grid>
</Window>
EOF
cat /tmp/MediaViewer.xaml
Ausgabe

<Window x:Class="DeepSeeArch.UI.MediaViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Media Viewer" Width="900" Height="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Media Viewer" FontSize="20" Margin="10"/>
        <Border Grid.Row="1" x:Name="ImageContainer" Visibility="Collapsed">
            <Image x:Name="ImageControl" Stretch="Uniform"/>
        </Border>
        <wv2:WebView2 Grid.Row="1" x:Name="VideoWebView" Visibility="Collapsed"/>
    </Grid>
</Window>